"""Programa de práctica con el modulo math"""

import math                 # from math import *

#1ra expresión.
(math.pow(3, 0.75) * math.sqrt(2)) / (math.exp(2) - 1)
# Notación punto para usar cada una de las funciones del módulo.

#2da expresión.
a = 4
b = 2
(a <= 2) and (b != 3)                               # Genera un error ya que a y b no tienen valor.

#3ra expresión.
x = 3
math.tan(math.pow(x, 3))

#4ta expresión.
(math.cos(math.pi - 2) - 3) / (math.tan(math.sqrt(2) + math.pow(0.2, 3)))

#5ta expresión.
(((math.sqrt(2) + 1) / (math.sin(2) - 0.1)) / (math.pow(x, 2) - 1)) + 2

#6ta expresión.
c = 10
not((b < 5) or (c == 0))

#





